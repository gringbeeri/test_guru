<%= form_with(model: [@test, @question], local: true) do |form| %>
  <p>
    <%= form.label :body %><br>
    <%= form.text_field :body, id: :question_body %>
  </p><br>

  <%= form.hidden_field :test_id, value: params[:test_id] %>

  <p><%= form.submit %></p>
<% end %>
